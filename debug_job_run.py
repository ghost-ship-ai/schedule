#!/usr/bin/env python3
"""
Debug what happens when a job runs during DST transition.
"""

import datetime
import os
import time
import schedule
import pytz


class mock_datetime:
    """
    Monkey-patch datetime for predictable results
    """

    def __init__(self, year, month, day, hour, minute, second=0, zone=None, fold=0):
        self.year = year
        self.month = month
        self.day = day
        self.hour = hour
        self.minute = minute
        self.second = second
        self.zone = zone
        self.fold = fold
        self.original_datetime = None
        self.original_zone = None

    def __enter__(self):
        class MockDate(datetime.datetime):
            @classmethod
            def today(cls):
                return cls(self.year, self.month, self.day)

            @classmethod
            def now(cls, tz=None):
                mock_date = cls(
                    self.year,
                    self.month,
                    self.day,
                    self.hour,
                    self.minute,
                    self.second,
                    fold=self.fold,
                )
                if tz:
                    return mock_date.astimezone(tz)
                return mock_date

        self.original_datetime = datetime.datetime
        datetime.datetime = MockDate

        self.original_zone = os.environ.get("TZ")
        if self.zone:
            os.environ["TZ"] = self.zone
            time.tzset()

        return MockDate(
            self.year, self.month, self.day, self.hour, self.minute, self.second
        )

    def __exit__(self, *args, **kwargs):
        datetime.datetime = self.original_datetime
        if self.original_zone:
            os.environ["TZ"] = self.original_zone
        elif "TZ" in os.environ:
            del os.environ["TZ"]
        time.tzset()


def debug_job_execution():
    """
    Debug what happens when a job executes during DST transition.
    """

    def mock_job():
        print("Job executed!")
        return "executed"

    schedule.clear()

    print("=== Debugging Job Execution During DST ===")

    # Start before the DST transition
    with mock_datetime(2023, 10, 29, 1, 0, zone="Europe/Berlin"):
        job = schedule.every().day.at("02:30", "Europe/B
import pytz


class mock_datetime:
    """
    Monkey-patch datetime for predictable results
    """

    def __init__(self, year, month, day, hour, minute, second=0, zone=None, fold=0):
        self.year = year
        self.month = month
        self.day = day
        self.hour = hour
        self.minute = minute
        self.second = second
        self.zone = zone
        self.fold = fold
        self.original_datetime = None
        self.original_zone = None

    def __enter__(self):
        class MockDate(datetime.datetime):
            @classmethod
            def today(cls):
                return cls(self.year, self.month, self.day)

            @classmethod
            def now(cls, tz=None):
                mock_date = cls(
                    self.year,
                    self.month,
                    self.day,
                    self.hour,
                    self.minute,
                    self.second,
                    fold=self.fold,
                )
                if tz:
                    return mock_date.astimezone(tz)
                return mock_date

        self.original_datetime = datetime.datetime
        datetime.datetime = MockDate

        self.original_zone = os.environ.get("TZ")
        if self.zone:
            os.environ["TZ"] = self.zone
            time.tzset()

        return MockDate(
            self.year, self.month, self.day, self.hour, self.minute, self.second
        )

    def __exit__(self, *args, **kwargs):
        datetime.datetime = self.original_datetime
        if self.original_zone:
            os.environ["TZ"] = self.original_zone
        elif "TZ" in os.environ:
            del os.environ["TZ"]
        time.tzset()


def test_fold_issue_in_correct_utc_offset():
    """
    Test the fold issue in _correct_utc_offset method.

    The issue is that when we have an ambiguous time during DST fall-back,
    the method might not properly handle the fold attribute.
    """

    def mock_job():
        return "executed"

    schedule.clear()

    print("=== Testing fold issue in _correct_utc_offset ===")

    # Create a job
    job = schedule.every().day.at("02:30", "Europe/Berlin").do(mock_job)

    berlin_tz = pytz.timezone("Europe/Berlin")

    # Create a datetime during the DST overlap with wrong UTC offset
    # This simulates what might happen when adding a day to a time during DST

